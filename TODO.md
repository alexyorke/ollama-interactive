# TODO

Current roadmap:

- [ ] Keep `scripts/nightly_self_improvement_report.py` green and use `scratch/nightly-self-improvement/*/report.json` as the primary measurement artifact.
- [ ] Continue reducing no-LLM probe cost based on the latest nightly. `scratch/nightly-self-improvement/20260629T050202Z/report.json` is green and compares against the prior timestamped run; the noisy cold pass has `discover_validators` at about `87 ms`, `fts_refresh` at about `54 ms`, and cached validation/FTS rows still much cheaper than cold rows. The focused validator-discovery probe improved from `169/114 ms` cold/repeat in `scratch/validation/discover-validators-synthetic-20260629.json` to `70/64 ms` in `scratch/validation/discover-validators-synthetic-after-availability-defer.json` after deferring availability checks to the selected validator slice, then to `80/25 ms` in `scratch/validation/discover-validators-synthetic-after-python-tool-cache.json` after caching optional Python tool resolution per executor. The current command-resolution cache keeps cold `discover_validators` noisy but reduces repeated same-executor discovery from `69 ms` to about `2 ms` in `scratch/validation/discover-validators-repeat-after-which-cache.json`; the broad probe in `scratch/validation/tool-speed-probe-after-which-cache-v2.json` also shows cached `lint_typecheck` at about `2 ms`. The broad speed probe in `scratch/validation/tool-speed-probe-after-native-ast-search.json` reduced the common Python function-pattern `ast_search` row from `82 ms` to `27 ms` by using a native AST path when `ast-grep` is installed.
- [ ] Use the refreshed local trajectory corpus and `scratch/validation/trajectory-evidence-coderforge-500.*` to choose the next bounded controller tranche instead of relying on historical gaps. The latest CoderForge sample points first at context-loop reduction: 500 rows, 70.63 average tool calls, 44.4% context-loop rows, and 4.8% edit-without-later-test rows. The first follow-up route now promotes single-code-file `list_files` output to `code_outline`, normalizes simple shell inspection (`cat`/`ls`/`grep`/`rg`/`head`/`tail`/simple `find -name`) to structured `read_file`/`list_files`/`search`/`file_search`/`directory_search`, proactive post-edit validation failures now echo compact diagnostics before forcing repair, explicit path mutations now require target-path grounding instead of unrelated context, and repeated `run_shell` syntax failures now stop before a third duplicate retry with temp-script/simpler-command guidance. The refreshed evidence report now also counts shell-specific failures in fix coverage, raising `validated-cli-preflight` to `325` hits and `path-repair-guard` to `203`; keep the next controller tranche equally narrow and evidence-backed.
- [ ] Use the June 29, 2026 cross-dataset trajectory pass as the next controller ranking input, not just CoderForge. `scratch/external/datasets/trajectory-profile-current.json`, `trajectory-error-profile-current.json`, `trajectory-evidence-report-current.json`, and `trajectory-evidence-report-terminalbench-500.json` now agree on the next three measured gaps: repeated `search/read` loops in SWE-agent traces, repeated `execute_bash` plus edit churn in OpenHands-style traces, and missing post-edit validation in TerminalBench-style shell repair (`96` edit-without-test rows in the 500-row sample). Keep the next controller tranche limited to one of those families at a time.
- [ ] Add a bounded dependency-bootstrap recovery path before broader shell heuristics. The cleaned TerminalBench shell-intent sample still leaves `843` `other-shell` commands, and the top surviving examples are ad hoc `wget`/`curl`/manual package-fetch flows for missing runtime dependencies. Prefer deterministic install or recovery guidance over letting the model improvise repeated download commands.
- [ ] Promote the new real-user trace evidence into the next controller tranche instead of treating real-world usage as missing. The June 29, 2026 local `trace-commons/agent-traces` sample is small (`30` sessions) but already shows a distinct waste shape: `142.13` average tool calls, `27/30` sessions with repeated identical tool pairs, `30/30` sessions with no explicit test command by simple shell-command matching, and dominant tool families `Edit=972`, `Read=847`, `Bash=794`, `PowerShell=521`. Use that to prioritize one bounded follow-up family at a time: greenfield/startup prompt slimming, repeated `Read`/`Edit`/shell loop compression, or stronger post-edit validation prompts for shell-heavy sessions.
- [ ] Keep looking for additional ungated real-user coding-agent transcript corpora, but stop waiting on them before acting. `trace-commons/agent-traces` is now downloaded locally, `thoughtworks/agentic-coding-trajectories` is downloaded as a broader cross-framework comparison set, and `SALT-NLP/SWE-chat` remains gated as of June 29, 2026.
- [ ] Use the new shell command-shape counts in `scratch/validation/trajectory-evidence-coderforge-500.*` before adding more shell routing. The current 500-row sample reports top shell families `python=9964`, `grep=4749`, `find=1719`, and `git=496`, with top shapes including `python ARG`, `grep -n ARG PATH`, `python -c ARG`, and `find ... -exec grep -l ...`. Direct follow-up now normalizes `grep -n` and `--line-number` inspection to structured `search`, and normalizes exact `find <path> -name <glob> [-type f] -exec grep -l <query> {} ;` inspection to filtered `search` with the filename glob preserved. Unsupported grep flags and broader shell pipelines still stay on `run_shell`; continue focusing on measured Python/test or grep/find shapes rather than broad shell normalization.
- [ ] Keep Python command routing narrow and validation-oriented. The evidence sample's `python -m pytest ...` and unittest-like shapes now route through `run_test` even when no configured default test command exists, preserving the original command; exact user requests for `run_shell` still stay on `run_shell`. Do not route arbitrary `python script.py` or `python -c ...` snippets to validation until the evidence distinguishes reproducible tests from exploratory scripts.
- [ ] Use shell command intent counts, not family counts alone, before adding more Python routing. A regression test now proves the evidence report separates `python-test-command`, `python-inline-probe`, `python-script-run`, `find-grep-search`, and plain `text-search`; this prevents treating exploratory `python -c` probes as validation commands.
- [ ] Measure before adding more shell-routing heuristics. The post-shell-routing context slice in `scratch/coding-benchmark/context-shell-routing-slice-after-directory-search.json` passed `3/3` with `3` LLM calls and `3307` total tokens; compared with `local-full-granite-after-target-path-grounding.json`, deltas were `multi_file_refactor +0.6%`, `docs_sync_after_api_change +1.0%`, and `nested_package_import_fix -0.9%`. The full `local-small` gate in `scratch/coding-benchmark/local-small-granite-after-shell-routing.json` passed `8/8` with `4` LLM calls and `2058` total tokens, with no meaningful regression versus `local-small-granite-after-target-path-grounding.json`.
- [ ] Keep the June 29, 2026 live gate current for `granite4.1:8b`, `gemma4:e4b`, and `qwen3:8b`; preserve Granite as the default unless a fresh gate beats it on pass rate or token tie-breaks. Latest gate: `scratch/live-model-gate-20260629-local-small/live-model-gate-summary.json`; all three passed `local-small`, and Granite stayed default with the fewest benchmark tokens (`2047` vs `2435` and `2534`).
- [ ] Re-run `local-full` on the chosen default after meaningful controller or validator changes, with explicit attention on multi-file refactor behavior whenever grounding logic changes. Latest passing full sweep: `scratch/coding-benchmark/local-full-granite-after-target-path-grounding.json` on June 29, 2026; it stayed `33/33` passing with `12` LLM calls and `6847` total tokens after explicit target-path grounding, a small safety cost versus the prior `6828`-token full sweep.
- [ ] Preserve optional-tool fail-closed behavior and install guidance for `actionlint`, `ShellCheck`, `tree-sitter`, `ast-grep`, and `Semgrep`-compatible scans. Current local doctor reports actionlint, ShellCheck, py-tree-sitter, and ast-grep installed; `semgrep_scan` now accepts Opengrep as a Semgrep-compatible CLI fallback and reports both compatible install paths when neither scanner is available.
